package com.softserve.edu4;

public class AppStudent {
	public static void main(String[] args) {
		Student s1 = new Student("Ivan", 1);
		/*
		Student s2 = new Student("Petro", 2);
		Student s3 = new Student("Stepan", 3);
		//
		Student betterStudent = s1;
		if (betterStudent.getRating() < s1.getRating()) {
			betterStudent = s1;
		}
		if (betterStudent.getRating() < s2.getRating()) {
			betterStudent = s2;
		}
		if (betterStudent.getRating() < s3.getRating()) {
			betterStudent = s3;
		}
		System.out.println("Avg of Students = " + Student.getAvgRating());
		System.out.println("betterStudent = " + betterStudent);
		*/
		System.out.println("The end");
	}
}
